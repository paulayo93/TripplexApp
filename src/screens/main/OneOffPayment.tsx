import React from 'react';
import { Layout, Text } from '@ui-kitten/components';

const OneOffPayment = () => {
  return (
    <Layout style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text category="h1">OneOffPayment</Text>
    </Layout>
  );
};

export default OneOffPayment;
